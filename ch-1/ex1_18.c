#include <stdio.h>
#define MAXLINE 1000

int main(void) {
    // get line and save it into an array char by char

    char arr[];
    char c;

    while((c = getchar()) != EOF) {
        if (c != '\n') {
            for (int i = 0; i < MAXLINE; i++) 
        }
}
